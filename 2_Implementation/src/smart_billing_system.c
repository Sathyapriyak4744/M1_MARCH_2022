#include"main.h"
{
void gotoxy(int x,int y)

COORD coord = {0,0};
coord.X = x;
    coord.Y = y;

    void gotoxy(int x,int y)

    int num;
float price;
float total;
char choice;
int again;

mainmenu()

 void chat()

void exit(back)
	   getch();
  exit(0);
}

